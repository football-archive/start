---
// src/pages/index.astro
import BaseLayout from "../layouts/BaseLayout.astro";

import { loadCallups } from "../lib/callups";
import { loadClubSquads } from "../lib/clubSquads";
import { loadLeagueMaster } from "../lib/leagueMaster";
import { loadClubMaster } from "../lib/clubMaster";
import { loadUpdates } from "../lib/updates";
import {
  clubsListUrl,
  tournamentTopUrl,
  DEFAULTS,
  competitionSeasonUrl,
} from "../lib/urls";
import { getUclSeasons, loadUclLeaguePhaseBySeason } from "../lib/ucl";
import { loadTransfers } from "../lib/transfers";
import { toYMD } from "../lib/rosterFormat";
import { loadLeagueStandings } from "../lib/leagueStandings";

const wcYear = String(DEFAULTS.currentWcYear); // 最新大会のYYYY

// yyyy-mm-dd の最大値を返す（空なら null）
const maxYmd = (rows: any[], key: string) => {
  let max = "";
  for (const r of rows) {
    const v = r?.[key];
    if (!v) continue;

    const s = toYMD(String(v));
    if (!s) continue;

    if (!max || s > max) max = s;
  }
  return max || null;
};

// 表示用（yyyy/mm/dd）
const ymdToJa = (ymd: any) => (ymd ? String(ymd).replaceAll("-", "/") : "—");

// --------------------
// 更新情報
// --------------------
const updates = loadUpdates(12);

const groupedUpdates = Object.entries(
  updates.reduce((acc: Record<string, any[]>, u: any) => {
    const key = String(u.date ?? "").trim();
    if (!key) return acc;
    if (!acc[key]) acc[key] = [];
    acc[key].push(u);
    return acc;
  }, {}),
).sort((a, b) => (a[0] < b[0] ? 1 : -1));

// --------------------
// 代表（WC2026 / 日本）
// --------------------
const callupsAll = loadCallups();
const callupsWc = callupsAll.filter(
  (r: any) => r.competition === "WC" && String(r.edition) === wcYear,
);

const japanHref = `/wc/${wcYear}/team/日本`;
const japanNames = new Set(["日本", "日本代表", "Japan"]);
const callupsJapan = callupsWc.filter((r: any) =>
  japanNames.has(String(r.country ?? "").trim()),
);
const japanUpdatedAt = ymdToJa(maxYmd(callupsJapan, "snapshot_date"));

const wcUpdatedAt = ymdToJa(maxYmd(callupsWc, "snapshot_date"));

// チーム数：country で distinct（空は除外）
const wcTeamCount = new Set(
  callupsWc.map((r: any) => String(r.country ?? "").trim()).filter(Boolean),
).size;

// 人数：行数（今のCSV構造だと「選手1行」なので length でOK）
const wcPlayerCount = callupsWc.length;

// --------------------
// クラブ（公開リーグ×公開クラブ、最新だけ集計）
// --------------------
const leagueShortcuts = [
  { label: "プレミアリーグ", league: "premier-league" },
  { label: "ラ・リーガ", league: "laliga" },
  { label: "ブンデスリーガ", league: "bundesliga" },
  { label: "セリエA", league: "serie-a" },
  { label: "リーグ・アン", league: "ligue-1" },
 // { label: "エールディヴィジ", league: "eredivisie" },
];

// ✅ is_publicの型ブレ吸収
const isTrue = (v: any) => {
  if (typeof v === "boolean") return v;
  const s = String(v ?? "")
    .trim()
    .toLowerCase();
  return s === "true" || s === "1" || s === "yes" || s === "y" || s === "t";
};

// league_key 正規化（/clubs と同じ思想）
const leagueMasterRows = loadLeagueMaster();
const leagueAliasToKey = new Map<string, string>();
for (const l of leagueMasterRows as any[]) {
  const key = String(l.league_key ?? "").trim();
  if (!key) continue;
  const aliases = [
    l.league_key,
    l.league,
    l.league_display_ja,
    l.league_display_en,
  ];
  for (const a of aliases) {
    const s = String(a ?? "").trim();
    if (!s) continue;
    leagueAliasToKey.set(s, key);
    leagueAliasToKey.set(s.toLowerCase(), key);
  }
}
function normalizeLeagueKey(v: any): string {
  const s = String(v ?? "").trim();
  if (!s) return "";
  return leagueAliasToKey.get(s) ?? leagueAliasToKey.get(s.toLowerCase()) ?? s;
}

// 公開リーグ（正規化後キー）
const publicLeagueKeys = new Set(
  (leagueMasterRows as any[])
    .filter((l) => isTrue(l.is_public))
    .map((l) => normalizeLeagueKey(l.league_key)),
);

// 公開クラブ（inactive / is_public=false を除外）
const clubMasterRows = loadClubMaster();
const publicClubKeys = new Set(
  (clubMasterRows as any[])
    .filter((c) => {
      if ((c.status ?? "active") === "inactive") return false;
      if (!isTrue(c.is_public ?? true)) return false;
      return true;
    })
    .map((c) => String(c.club_key ?? "").trim())
    .filter(Boolean),
);

const clubRowsAll = loadClubSquads();

// 公開リーグ × 公開クラブの行だけ
const clubRowsPublic = clubRowsAll.filter((r: any) => {
  const lk = normalizeLeagueKey(r.league_key ?? r.league);
  const ck = String(r.club_key ?? "").trim();
  if (!lk || !ck) return false;
  if (!publicLeagueKeys.has(lk)) return false;
  if (!publicClubKeys.has(ck)) return false;
  return true;
});

function seasonStartYear(season: any): number {
  const s = String(season ?? "").trim();
  const m = s.match(/(\d{4})/);
  return m ? Number(m[1]) : 0;
}
function windowRank(w: any): number {
  const s = String(w ?? "")
    .trim()
    .toLowerCase();
  return s === "winter" ? 2 : 1; // summer or empty
}

type LatestMeta = { season: string; window: string; snapshot_date: string };

function isNewerMeta(a: LatestMeta, b: LatestMeta): boolean {
  const ay = seasonStartYear(a.season);
  const by = seasonStartYear(b.season);
  if (ay !== by) return ay > by;

  const aw = windowRank(a.window);
  const bw = windowRank(b.window);
  if (aw !== bw) return aw > bw;

  return String(a.snapshot_date ?? "") > String(b.snapshot_date ?? "");
}

const clubKeyOf = (r: any) =>
  `${normalizeLeagueKey(r.league_key ?? r.league)}__${String(r.club_key ?? "").trim()}`;

const clubLatestMeta = new Map<string, LatestMeta>();
for (const r of clubRowsPublic) {
  const k = clubKeyOf(r);
  if (!k) continue;

  const meta: LatestMeta = {
    season: String(r.season ?? "").trim(),
    window: String(r.window ?? "")
      .trim()
      .toLowerCase(),
    snapshot_date: String(r.snapshot_date ?? "").trim(),
  };

  if (meta.window !== "summer" && meta.window !== "winter") meta.window = "";
  if (!meta.snapshot_date) continue;

  const prev = clubLatestMeta.get(k);
  if (!prev || isNewerMeta(meta, prev)) clubLatestMeta.set(k, meta);
}

const clubRowsPublicLatest = clubRowsPublic.filter((r: any) => {
  const k = clubKeyOf(r);
  const latest = clubLatestMeta.get(k);
  if (!latest) return false;

  const meta: LatestMeta = {
    season: String(r.season ?? "").trim(),
    window: String(r.window ?? "")
      .trim()
      .toLowerCase(),
    snapshot_date: String(r.snapshot_date ?? "").trim(),
  };
  if (meta.window !== "summer" && meta.window !== "winter") meta.window = "";

  return (
    meta.season === latest.season &&
    meta.window === latest.window &&
    meta.snapshot_date === latest.snapshot_date
  );
});

const clubsLeagueCount = new Set(
  clubRowsPublicLatest.map((r: any) =>
    normalizeLeagueKey(r.league_key ?? r.league),
  ),
).size;

const clubsClubCount = clubLatestMeta.size;
const clubsPlayerCount = clubRowsPublicLatest.length;

const standingRowsAll = loadLeagueStandings();

// トップで見せたい対象リーグ
const topClubLeagueKeys = new Set([
  "premier-league",
  "laliga",
  "bundesliga",
  "serie-a",
  "ligue-1",
  "eredivisie",
]);

const standingRowsTop = standingRowsAll.filter((r: any) => {
  const lk = normalizeLeagueKey(r.league_key);
  return topClubLeagueKeys.has(lk);
});

const standingUpdatedMax = maxYmd(standingRowsTop, "updated_at");
const updatedAtClub = ymdToJa(standingUpdatedMax);

const leaguesInData = new Set(
  clubRowsPublicLatest.map((r: any) =>
    normalizeLeagueKey(r.league_key ?? r.league),
  ),
);

const leagueShortcutsVisible = leagueShortcuts.filter(
  (x) => publicLeagueKeys.has(x.league) && leaguesInData.has(x.league),
);

const leagueHref = (leagueKey: string) => `/clubs/${leagueKey}`;

// --------------------
// UCL
// --------------------
const uclSeasons = getUclSeasons();
const uclSeason = uclSeasons?.[0] ? String(uclSeasons[0]).trim() : "2025-26";
const uclHref = competitionSeasonUrl("ucl", uclSeason);
const uclRows = loadUclLeaguePhaseBySeason(uclSeason);
const uclUpdatedAt = uclRows?.[0]?.updated_at
  ? String(uclRows[0].updated_at).replaceAll("-", "/")
  : "—";
const uclClubCount = uclRows?.length ?? 0;

// --------------------
// Transfers
// --------------------
const transfersAll = loadTransfers();
const transfersUpdatedAt = ymdToJa(maxYmd(transfersAll, "date_iso"));

// WC Top（リンクに必要な場合用。現状 index では未使用でもOK）
const wcTopHref = tournamentTopUrl({
  tournament: DEFAULTS.currentWcTournament,
  year: DEFAULTS.currentWcYear,
});

// いったん準備できた大会の入口
const wcPastEditions = ["2022", "2018", "2014", "2010", "2006", "2002"];
---

<BaseLayout
  title="海外サッカーDB｜W杯2026出場国・代表メンバー一覧"
  bodyClass="page-home"
  description="FIFAワールドカップ2026の出場国・代表メンバー一覧を中心に、各国代表の背番号・所属クラブ・過去大会の情報をまとめた海外サッカーデータベースサイトです。海外クラブ登録メンバーやワールドカップ情報も一覧できます。"
  bodyStyle="margin:0; background:#fafafa; color:#111;"
>
  <style is:global>
    :root {
      --border: #e6e6e6;
      --muted: #666;
      --bg: #fafafa;
      --date-red: #e11d48;
    }

    /* ==========================
       Layout
    ========================== */
    .container {
      max-width: var(--container-max);
      margin: 0 auto;
      padding: 12px var(--container-pad) 48px;
    }
    .hero {
      background: #fff;
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 14px 18px;
    }
    .subtitle {
      margin: 0 0 14px;
      color: var(--muted);
      line-height: 1.6;
      font-size: 14px;
    }

    /* 2列×3行（PC） */
    .grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 12px;
      margin-top: 12px;
    }
    @media (min-width: 760px) {
      .grid {
        grid-template-columns: 1fr 1fr;
        grid-template-rows: auto auto auto;
        column-gap: 12px;
        row-gap: 12px;
        align-items: stretch;
      }
      .grid-nt-main {
        grid-column: 1;
        grid-row: 1;
      }
      .grid-club-main {
        grid-column: 2;
        grid-row: 1;
      }
      .grid-nt-sub1 {
        grid-column: 1;
        grid-row: 2;
      }
      .grid-club-sub1 {
        grid-column: 2;
        grid-row: 2;
      }
      .grid-nt-sub2 {
        grid-column: 1;
        grid-row: 3;
      }
      .grid-club-sub2 {
        grid-column: 2;
        grid-row: 3;
      }
    }

    /* ==========================
       Common UI
    ========================== */
    .date-red {
      color: var(--date-red);
      font-weight: 700;
    }

    .updates {
      margin-top: 10px;
      padding: 12px 14px;
      border: 1px solid var(--border);
      border-radius: 14px;
      background: #fff;
    }
    .updates h2 {
      margin: 0 0 6px;
      font-size: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .updates ul {
      margin: 0;
      padding-left: 16px;
    }
    .updates li {
      margin: 4px 0;
      line-height: 1.5;
      font-size: 12.5px;
    }

    .updates {
      margin-top: 10px;
      padding: 12px 14px;
      border: 1px solid var(--border);
      border-radius: 14px;
      background: #fff;
    }

    .updates h2 {
      margin: 0 0 8px;
      font-size: 15px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .updates-groups {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .updates-group {
      padding-top: 2px;
    }

    .updates-group-date {
      color: var(--date-red);
      font-weight: 800;
      font-size: 13px;
      line-height: 1.4;
      margin-bottom: 4px;
      font-variant-numeric: tabular-nums;
    }

    .updates-group-list {
      margin: 0;
      padding-left: 16px;
    }

    .updates-group-list li {
      margin: 3px 0;
      line-height: 1.5;
      font-size: 13px;
    }

    .updates .title {
      font-weight: 400;
      font-size: 14px;
    }

    .updates .title-link {
      color: inherit;
      text-decoration: underline;
      text-underline-offset: 3px;
    }

    .updates .title-link:hover {
      opacity: 0.82;
    }

    @media (max-width: 640px) {
      .updates {
        padding: 10px 12px;
      }

      .updates h2 {
        font-size: 14px;
        margin-bottom: 6px;
      }

      .updates-groups {
        gap: 8px;
      }

      .updates-group-date {
        font-size: 12.5px;
        margin-bottom: 3px;
      }

      .updates-group-list {
        padding-left: 14px;
      }

      .updates-group-list li {
        margin: 2px 0;
        font-size: 12.5px;
        line-height: 1.45;
      }

      .updates .title {
        font-size: 13px;
      }
    }

    .updates-group-list li:first-child .title {
      font-weight: 600;
    }

    .quick-title {
      margin: 0;
      font-size: 13px;
      color: var(--muted);
    }

    .pills {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }
    .pill {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 7px 10px;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: #fff;
      color: #111;
      text-decoration: none;
      font-size: 13px;
      line-height: 1;
      transition:
        transform 0.06s ease,
        box-shadow 0.06s ease;
    }
    .pill:hover {
      transform: translateY(-1px);
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
    }
    .pill .dot {
      width: 7px;
      height: 7px;
      border-radius: 999px;
      background: #111;
      opacity: 0.35;
    }

    .see-now {
      font-size: 13px;
      color: #111;
      text-decoration: none;
      white-space: nowrap;
    }
    .see-now:hover {
      text-decoration: underline;
    }

    .badge {
      font-size: 12px;
      color: #222;
      border: 1px solid var(--border);
      padding: 3px 8px;
      border-radius: 999px;
      background: #fff;
      white-space: nowrap;
    }

    /* ==========================
       Cards (main)
    ========================== */
    .card {
      background: var(--theme-soft-bg);
      border: 2px solid var(--theme-soft-border);
      border-radius: 14px;
      padding: 16px;
      color: inherit;
      display: block;
      transition:
        transform 0.06s ease,
        box-shadow 0.06s ease;
      cursor: default;
    }
    .card:hover {
      transform: translateY(-1px);
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.06);
    }

    .card-title {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 10px;
      margin: 0 0 6px;
      flex-wrap: wrap;
    }

    /* ✅ タイトル統一（WC/クラブ/サブすべて 18px） */
    body.page-home .card h2,
    body.page-home .subcard-title {
      font-size: 18px;
      font-weight: 800;
      margin: 0 0 8px;
      line-height: 1.25;
    }

    .card-desc {
      margin: 0;
      color: #555;
      line-height: 1.6;
    }
    .card-meta {
      margin-top: 10px;
      color: var(--muted);
      font-size: 12px;
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }
    /* “線の下に余白”が欲しければ、線の次のブロックにmargin-topを付ける */
    .card-portal .portal-links,
    .card-club .card-shortcuts {
      margin-top: 12px;
    }
    .card-shortcuts {
      border-top: none;
      padding-top: 0;
    }
    .shortcut-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin: 0 0 10px;
      flex-wrap: wrap;
    }

    /* ==========================
       カード上部高さ統一
    ========================== */
    .card-head {
      min-height: 72px; /* ←ここで高さ統一 */
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    /* 行間を揃える */
    .card-head p {
      margin: 0;
      line-height: 1.45;
    }

    /* meta 行も揃える */
    .card-head .card-meta {
      margin-top: 6px;
    }

    /* ==========================
       WC portal (dark face)
    ========================== */
    .card-portal {
      color: #fff;
      border-color: color-mix(in srgb, var(--theme-color) 70%, #111);
      background: linear-gradient(
        135deg,
        color-mix(in srgb, var(--theme-color) 85%, #000),
        color-mix(in srgb, var(--theme-color) 75%, #ffffff)
      );
      border-radius: 14px;
      padding: 16px;
    }
    .card-portal h2 {
      margin: 0 0 12px;
      font-size: 1.3rem; /* 見出しの存在感だけはWCポータル特別 */
    }

    /* タイトル下の余白を統一（ここが効く） */
    .page-home .card-h2 {
      display: flex;
      align-items: center;
      gap: 8px;
      margin: 0 0 4px; /* ←ここが“空きすぎ”の本丸 */
    }

    .page-home .card-ico {
      display: inline-flex;
      width: 22px;
      justify-content: center;
    }

    /* ==========================
       カード上部高さ（カード別に統一）
    ========================== */
    .card-head {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .card-portal .card-head {
      min-height: 72px;
    }
    .card-club .card-head {
      min-height: 76px;
    } /* まずは同値で揃える */

    /* 行間を揃える */
    .card-head p {
      margin: 0;
      line-height: 1.45;
    }
    .card-head .card-meta {
      margin-top: 6px;
    }

    /* 区切り線は card-sep 1本に統一 */
    .card-sep {
      margin-top: 8px;
      border-top: 1px dashed var(--border);
    }

    /* WC内のmetaは白寄せ（※線は持たせない） */
    .card-portal .card-meta {
      color: rgba(255, 255, 255, 0.88);
      padding-bottom: 0;
      margin-bottom: 0;
      border: none;
    }
    .card-portal .card-meta .date-red {
      color: #ffd400;
      font-weight: 800;
    }

    .card-portal .card-meta .date-red {
      color: #ffd400; /* WCの中では赤より黄が視認性高い */
      font-weight: 800;
    }

    .card-portal .card-meta .badge {
      background: rgba(255, 255, 255, 0.12);
      color: rgba(255, 255, 255, 0.92);
      border-color: rgba(255, 255, 255, 0.25);
    }

    .portal-links {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .portal-sub-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 8px;
    }

    .portal-sub {
      width: 100%;
      text-align: center;
    }

    .portal-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      text-decoration: none;
      transition:
        transform 0.08s ease,
        background 0.15s ease,
        box-shadow 0.15s ease,
        opacity 0.15s ease;
    }

    .portal-btn:hover {
      transform: translateY(-1px);
    }

    .portal-main {
      background: #fff;
      color: #111;
      font-weight: 800;
      font-size: 15px;
      line-height: 1.2;
      padding: 10px 14px;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.12);
    }

    .portal-main:hover {
      background: rgba(255, 255, 255, 0.92);
    }

    .portal-sub {
      background: rgba(255, 255, 255, 0.14);
      color: #fff;
      font-weight: 700;
      font-size: 12px;
      line-height: 1.2;
      padding: 6px 8px;
      opacity: 0.95;
      width: 100%;
      text-align: center;
    }

    .portal-sub:hover {
      background: rgba(255, 255, 255, 0.24);
    }

    @media (max-width: 768px) {
      .grid-nt-main .wc-past-links,
      .grid-nt-main > .legend-btn {
        display: none;
      }

      .wc-mobile-more {
        display: block;
        margin-top: 8px;
        font-size: 13px;
      }

      .wc-mobile-more a {
        color: rgba(255, 255, 255, 0.85);
        font-size: 12px;
      }
    }

    /* ==========================
       Subcards
    ========================== */
    .subcard {
      background: var(--theme-soft-bg);
      border: 1px solid var(--theme-soft-border);
      border-radius: 14px;
      padding: 16px;

      /* サブカードは少し濃い目に（差が出るように） */
      --theme-soft-bg: color-mix(in srgb, var(--theme-color) 28%, #ffffff);
      --theme-soft-border: color-mix(
        in srgb,
        var(--theme-color) 48%,
        var(--border)
      );
    }

    /* ✅ タイトルは h3 + a で統一（下線は必要なので標準でON） */
    .subcard-title a {
      color: inherit;
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    /* 補助文 */
    .ucl-sub,
    .jp-sub,
    .transfer-sub,
    .euro-sub {
      margin-top: 6px;
      margin-bottom: 8px;
      font-size: 12px;
      line-height: 1.5;
      color: var(--muted);
    }

    /* EURO（リンク無し）も同じ見た目にするため、タイトル用クラスを廃止して subcard-title に寄せる */
    /* ※HTML側で subcard-title の中身を単なるテキストにしてOK */

    /* ==========================
       Dark subcards (UCL / Japan)
    ========================== */
    .subcard.theme-ucl,
    .subcard.theme-japan {
      color: #fff;
      border-color: rgba(0, 0, 0, 0.25);
      /* もう一段濃く */
      --theme-soft-bg: color-mix(in srgb, var(--theme-color) 70%, #ffffff);
      --theme-soft-border: color-mix(
        in srgb,
        var(--theme-color) 88%,
        var(--border)
      );
    }
    .subcard.theme-ucl .ucl-sub,
    .subcard.theme-japan .jp-sub {
      color: rgba(255, 255, 255, 0.85);
    }
    /* 濃色カード内リンクの下線色 */
    .subcard.theme-ucl .subcard-title a,
    .subcard.theme-japan .subcard-title a {
      text-decoration-color: rgba(255, 255, 255, 0.75);
    }
    .subcard.theme-ucl .subcard-title a:hover,
    .subcard.theme-japan .subcard-title a:hover {
      text-decoration-color: #fff;
    }

    /* Footer */
    .footer {
      margin-top: 18px;
      font-size: 12px;
      font-weight: 600;
      color: #111;
    }

    .jp-history {
      margin-top: 10px;
    }

    .jp-history a {
      display: inline-block;
      padding: 6px 12px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.18);
      font-weight: 700;
      font-size: 13px;
      text-decoration: none;
      transition: 0.15s ease;
    }

    .jp-history a:hover {
      background: rgba(255, 255, 255, 0.28);
      transform: translateY(-1px);
    }

    .legend-btn {
      display: block;
      margin-top: 10px;
      padding: 10px;
      text-align: center;
      font-weight: 700;
      background: #ffd400;
      color: #000;
      border-radius: 8px;
      text-decoration: none;
    }

    /* 日本カード内でも強豪国ボタンと同じ見た目にする（上書き優先） */
    .jp-box .legend-btn {
      background: #ffd400;
      color: #111;
      border: 1px solid rgba(0, 0, 0, 0.12);
      text-decoration: none;
    }

    /* もし青カード側の a の padding / radius が崩すなら念のため固定 */
    .jp-box .legend-btn {
      background: #ffd400 !important;
      color: #111 !important;

      display: block;
      text-align: center;

      font-size: 16px; /* ← ここが重要 */
      font-weight: 700; /* 太さ */
      line-height: 1; /* 高さブレ防止 */

      padding: 12px 16px; /* 強豪国と同じ体感サイズ */
      border-radius: 10px;
    }

    /* 共通：アイコンの見た目 */
    .card-ico {
      display: inline-block;
      margin-right: 6px;
      font-size: 0.95em;
      line-height: 1;
      vertical-align: -0.08em;
      opacity: 0.95;
    }

    /* 色分け（好みで調整OK） */
    .ico-wc {
      color: #d4af37;
    } /* ゴールド */
    .ico-jp {
      color: #d11a2a;
    } /* 日の丸レッド */
    .ico-club {
      color: #166534 !important;
    }
    .ico-ucl {
      color: #eab308;
    } /* ゴールド寄り */
    .ico-euro {
      color: #1f4ed8;
    } /* EUブルー */
    .ico-transfer {
      color: #ea580c;
    } /* オレンジ */

    .card-ico {
      font-family:
        "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", sans-serif;
    }

    .theme-club .card-h2 .card-ico {
      color: #166534 !important;
    }

    /* 👕だけは単色化しやすいフォントに寄せる（環境によっては効果あり） */
    .ico-club {
      font-family:
        "Segoe UI Symbol", "Noto Sans Symbols2", "Arial Unicode MS", sans-serif !important;
      color: #166534 !important;
    }

    .theme-wc {
      background: linear-gradient(180deg, #9f1d2c 0%, #7f1622 100%);
    }

    .theme-club {
      background: linear-gradient(180deg, #7fa293 0%, #5f8273 100%) !important;
    }

    .theme-japan {
      background: linear-gradient(180deg, #4f6fa3 0%, #324f80 100%) !important;
    }

    .theme-ucl {
      background: linear-gradient(180deg, #4c5b79 0%, #2f3e5a 100%) !important;
    }

    .theme-euro {
      background: linear-gradient(180deg, #cfd5de 0%, #aeb7c4 100%) !important;
    }

    .transfer-box {
      background: linear-gradient(180deg, #9fb8aa 0%, #7f9e90 100%);
    }

    .card,
    .subcard {
      position: relative;
      overflow: hidden;
      transition:
        transform 120ms ease,
        box-shadow 120ms ease;
    }

    .card:hover,
    .subcard:hover {
      transform: translateY(-2px);
      box-shadow: 0 12px 28px rgba(0, 0, 0, 0.15);
    }

    .card::before,
    .subcard::before {
      content: "";
      position: absolute;
      inset: 0;
      pointer-events: none;
      background: linear-gradient(
        180deg,
        rgba(255, 255, 255, 0.14) 0%,
        rgba(255, 255, 255, 0.04) 38%,
        rgba(0, 0, 0, 0.06) 100%
      );
    }

    /* ✅ WCカード内：代表ポータル導線 */
    .grid-nt-main .nt-portal-row {
      margin-top: 8px;
    }

    .grid-nt-main .nt-portal-btn {
      display: block;
      text-decoration: none;
      color: inherit;
      border: 1px solid rgba(0, 0, 0, 0.14);
      background: rgba(255, 255, 255, 0.12);
      border-radius: 12px;
      padding: 10px 12px;
      font-size: 13px;
      font-weight: 800;
      line-height: 1.35;
    }

    .grid-nt-main .nt-portal-btn:hover {
      border-color: color-mix(
        in srgb,
        var(--color-wc) 35%,
        rgba(0, 0, 0, 0.14)
      );
      box-shadow: 0 10px 22px rgba(0, 0, 0, 0.1);
    }

    /* ✅ WCカード内：過去大会ミニ導線 */
    .grid-nt-main .wc-past-links {
      margin-top: 8px;
      padding-top: 8px;
      border-top: 1px dashed rgba(255, 255, 255, 0.35);
    }

    .grid-nt-main .wc-past-head {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 8px;
      flex-wrap: wrap;
    }

    .grid-nt-main .wc-past-label {
      font-size: 13px;
      font-weight: 800;
      color: rgba(255, 255, 255, 0.9);
      letter-spacing: 0.02em;
    }

    .grid-nt-main .wc-past-note {
      font-size: 11px;
      color: rgba(255, 255, 255, 0.72);
    }

    .grid-nt-main .wc-past-pills {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .grid-nt-main .wc-past-pill {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 64px;
      padding: 8px 14px;
      border-radius: 999px;
      background: rgba(255, 255, 255, 0.2);
      border: 1px solid rgba(255, 255, 255, 0.26);
      color: #fff;
      text-decoration: none;
      font-size: 13px;
      font-weight: 800;
      line-height: 1;
      transition:
        transform 0.06s ease,
        background 0.12s ease,
        box-shadow 0.12s ease;
    }

    .grid-nt-main .wc-past-pill:hover {
      transform: translateY(-1px);
      background: rgba(255, 255, 255, 0.24);
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.12);
    }

    .jp-sub a {
      color: #fff;
      font-weight: 700;
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    .jp-sub a:hover {
      opacity: 0.9;
    }

    .jp-opponents {
      margin-top: 10px;
      font-size: 13px;
      line-height: 1.65;
    }

    .jp-opponents-title {
      font-weight: 700;
      margin: 8px 0 6px;
    }

    .jp-opponents-row {
      display: flex;
      flex-wrap: wrap;
      gap: 4px;
      align-items: center;
      margin-top: 2px;
    }

    .jp-opponents-row.small {
      font-size: 12px;
    }

    .jp-opponents-label {
      font-weight: 700;
      color: rgba(255, 255, 255, 0.9);
      margin-right: 2px;
    }

    .jp-opponents .sep {
      color: rgba(255, 255, 255, 0.45);
      margin: 0 2px;
    }

    .jp-opponents .muted {
      color: rgba(255, 255, 255, 0.82);
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    .jp-opponents a {
      color: #fff;
      font-weight: 700;
      text-decoration: underline;
      text-underline-offset: 2px;
    }

    .jp-opponents a:hover {
      opacity: 0.9;
    }

    .club-season-links {
      margin-top: 10px;
    }

    .season-link-row {
      display: flex;
      flex-wrap: wrap;
      gap: 8px 12px;
      margin-top: 4px;
    }

    .season-link {
      font-size: 13px;
      color: #111;
      text-decoration: underline;
      text-underline-offset: 3px;
    }

    .card-club .card-desc,
    .card-club .quick-title {
      color: rgba(17, 17, 17, 0.78);
    }

    .card-club .season-link {
      color: #163a2b;
      font-weight: 600;
      text-decoration: underline;
      text-underline-offset: 3px;
    }

    .card-club .season-link:hover {
      color: #0f2a1f;
    }

    .card-club .see-now {
      color: #163a2b;
      font-weight: 700;
    }

    .card-club .see-now:hover {
      color: #0f2a1f;
    }

    .card-club .club-season-links .quick-title {
      color: #163a2b;
      font-weight: 700;
    }
  </style>

  <div class="container">
    <section class="updates">
      <h2>🆕 更新情報</h2>
      {
        updates.length === 0 ? (
          <p class="muted">更新情報は準備中です。</p>
        ) : (
          <div class="updates-groups">
            {groupedUpdates.map(([date, items]) => (
              <section class="updates-group">
                <div class="updates-group-date">
                  {date.replaceAll("-", "/")}
                </div>

                <ul class="updates-group-list">
                  {items.map((u) => (
                    <li>
                      {u.href ? (
                        <a class="title-link" href={u.href}>
                          <span class="title">{u.title}</span>
                        </a>
                      ) : (
                        <span class="title">{u.title}</span>
                      )}
                    </li>
                  ))}
                </ul>
              </section>
            ))}
          </div>
        )
      }
    </section>

    <div class="hero">
      <p class="subtitle">
        海外サッカーの代表・クラブ・選手情報を一覧で見られるデータベースサイトです。<br
        />
        Wikiよりサッと見れて、Transfermarktより軽快。ニュースを追うより、全体が一目でわかる。<br
        />
        FIFAワールドカップの代表チームや、海外クラブ登録メンバーの「今 / あの時」を行き来する、世界サッカーDB。
      </p>

      <div class="grid">
        <!-- 🏆 WCポータルカード -->
        <div class="card card-portal theme-wc grid-nt-main">
          <h2 class="card-h2">
            <span class="card-ico ico-wc" aria-hidden="true">🏆</span>
            FIFAワールドカップ2026
          </h2>
          <div class="card-head">
            <p class="wc-desc">
              代表チームの最新メンバー、スケジュールはコチラから。
            </p>
            <div class="card-meta">
              <span>代表更新：<span class="date-red">{wcUpdatedAt}</span></span>
              <span>出場：{wcTeamCount}カ国 / {wcPlayerCount}選手</span>
            </div>
          </div>
          <div class="card-sep" aria-hidden="true"></div>

          <div class="portal-links">
            <a href="/wc/2026" class="portal-btn portal-main"
              >出場国・代表メンバー一覧</a
            >
            <a href="/wc/2026/#schedule" class="portal-btn portal-main"
              >試合日程・結果</a
            >

            <div class="portal-sub-grid">
              <a href="/wc/2026/#groups" class="portal-btn portal-sub"
                >グループリーグ勝敗表</a
              >
              <a href="/wc/2026/#rankings" class="portal-btn portal-sub"
                >得点・アシストランキング</a
              >
              <a href="/wc/2026/#awards" class="portal-btn portal-sub">表彰</a>
            </div>
          </div>

          <!-- ✅ 追加：代表ポータル導線（チーム軸の入口） -->
          <!-- ✅ 追加：代表ポータル導線（チーム軸の入口） -->
          <div class="nt-portal-row">
            <a class="nt-portal-btn" href="/nt">
              強豪国・日本代表・各国あゆみを見る
            </a>
          </div>

          <!-- ✅ 追加：過去大会ミニ導線 -->
          <div class="wc-past-links">
            <div class="wc-past-head">
              <div class="wc-past-label">過去大会</div>
              <div class="wc-past-note">大会ページを見る</div>
            </div>

            <div class="wc-past-pills">
              {
                wcPastEditions.map((edition) => (
                  <a
                    class="wc-past-pill"
                    href={tournamentTopUrl({ tournament: "wc", year: edition })}
                  >
                    {edition}
                  </a>
                ))
              }
            </div>
          </div>

          <a class="legend-btn" href="/nt/wc/legends">
            強豪国代表　W杯のあゆみ（歴代代表メンバー）
          </a>
        </div>

        <!-- row1 col2: CLUB main -->
        <div class="card card-club theme-club grid-club-main">
          <h2 class="card-h2">
            <span class="card-ico ico-club" aria-hidden="true">👕</span>
            2025-26 主要クラブ 登録メンバー(スカッド)
          </h2>
          <div class="card-head">
            <p class="card-desc">
              最新シーズンの登録選手を、クラブ別・リーグ別に。
            </p>
            <div class="card-meta">
              <span
                >順位表更新：<span class="date-red">{updatedAtClub}</span></span
              >
              <span>
                クラブ：{clubsLeagueCount}リーグ / {clubsClubCount}クラブ / {
                  clubsPlayerCount
                }選手
              </span>
            </div>
          </div>
          <div class="card-sep" aria-hidden="true"></div>

          <div class="card-shortcuts">
            <div class="shortcut-head">
              <p class="quick-title">⚡ショートカット：主要リーグから探す</p>
              <a class="see-now" href="/clubs">クラブ一覧へ →</a>
            </div>

            <div class="pills">
              {
                leagueShortcutsVisible.map((x) => (
                  <a
                    class="pill"
                    href={leagueHref(x.league)}
                    aria-label={`${x.label} のクラブ一覧へ`}
                  >
                    <span class="dot" aria-hidden="true" />
                    {x.label}
                  </a>
                ))
              }
              <a class="pill" href="/clubs" aria-label="全リーグのクラブ一覧へ">
                <span class="dot" aria-hidden="true"></span>
                全リーグ
              </a>
            </div>
            {/* ▼ 最新シーズン順位表 導線 */}
            <div class="club-season-links">
              <p class="quick-title">📊最新シーズン順位表：</p>
              <div class="season-link-row">
                {
                  leagueShortcutsVisible.map((x) => (
                    <a
                      href={`/clubs/${x.league}/${DEFAULTS.currentClubSeason}`}
                      class="season-link"
                    >
                      {x.label}
                    </a>
                  ))
                }
              </div>
            </div>
            <a class="legend-btn" href="/clubs/history">
              欧州強豪クラブのあゆみ（歴代メンバー）
            </a>
          </div>
        </div>

        <!-- row2 col1: Japan -->
        <div class="subcard jp-box theme-japan grid-nt-sub1">
          <h3 class="subcard-title">
            <a href={japanHref}>
              <span class="card-ico ico-jp" aria-hidden="true">🔴</span>
              SAMURAI BLUE（日本代表）
            </a>
          </h3>

          <div class="jp-sub">
            <a href="/wc/2026/team/日本">日本代表メンバー一覧</a><br />
            更新：<span class="date-red">{japanUpdatedAt}</span>
          </div>

          <div class="jp-opponents">
            <div class="jp-opponents-title">🔥 今後の対戦予定</div>

            <div class="jp-opponents-row">
              <span class="jp-opponents-label">3月：</span>
              <a
                href="/wc/2026/team/スコットランド"
                title="スコットランド代表メンバー一覧"
              >
                スコットランド
              </a>
              <span class="sep">｜</span>
              <a
                href="/wc/2026/team/イングランド"
                title="イングランド代表メンバー一覧"
              >
                イングランド
              </a>
              <span class="sep">｜</span>
              <span class="jp-opponents-label">5月：</span>
              <a
                href="/wc/2026/team/アイスランド"
                title="アイスランド代表メンバー一覧"
              >
                アイスランド
              </a>
            </div>

            <div class="jp-opponents-row">
              <span class="jp-opponents-label">W杯GL：</span>
              <a href="/wc/2026/team/オランダ" title="オランダ代表メンバー一覧">
                オランダ
              </a>
              <span class="sep">｜</span>
              <a
                href="/wc/2026/team/チュニジア"
                title="チュニジア代表メンバー一覧"
              >
                チュニジア
              </a>
              <span class="sep">｜</span>
              <a
                href="/wc/2026#teams"
                class="muted"
                title="W杯2026 出場国一覧へ"
              >
                欧州PO
              </a>
            </div>

            <div class="jp-opponents-row small">
              <a
                href="/wc/2026/team/ポーランド"
                title="ポーランド代表メンバー一覧"
              >
                ポーランド
              </a>
              <span class="sep">｜</span>
              <a
                href="/wc/2026/team/スウェーデン"
                title="スウェーデン代表メンバー一覧"
              >
                スウェーデン
              </a>
              <span class="sep">｜</span>
              <a
                href="/wc/2026/team/ウクライナ"
                title="ウクライナ代表メンバー一覧"
              >
                ウクライナ
              </a>
              <span class="sep">｜</span>
              <a
                href="/wc/2026/team/アルバニア"
                title="アルバニア代表メンバー一覧"
              >
                アルバニア
              </a>
            </div>
          </div>

          <!-- ★追加：歴代WC導線 -->
          <div class="jp-history">
            <a class="legend-btn" href="/nt/japan/wc"
              >日本代表　W杯のあゆみ（歴代代表メンバー）</a
            >
          </div>
        </div>

        <!-- row2 col2: UCL -->
        <div class="subcard ucl-box theme-ucl grid-club-sub1">
          <h3 class="subcard-title">
            <a href={uclHref}>
              <span class="card-ico ico-ucl" aria-hidden="true">👑</span>
              UEFAチャンピオンズリーグ(CL) {uclSeason}（順位表）
            </a>
          </h3>
          <div class="ucl-sub">
            リーグフェーズ最終順位表（クラブ詳細へ遷移できます）<br />
            更新：<span class="date-red">{uclUpdatedAt}</span>　参加：{
              uclClubCount
            }クラブ
          </div>
        </div>

        <!-- row3 col1: EURO -->
        <div class="subcard euro-box theme-euro grid-nt-sub2">
          <h3 class="subcard-title">
            <span class="card-ico ico-euro" aria-hidden="true">🌍</span>
            EURO 2028 予選
          </h3>
          <div class="euro-sub">準備中</div>
          <div class="euro-sub">
            更新：<span class="date-red">—</span>
          </div>
        </div>

        <!-- row3 col2: Transfers -->
        <div class="subcard transfer-box theme-club grid-club-sub2">
          <h3 class="subcard-title">
            <a href="/transfers">
              <span class="card-ico ico-transfer" aria-hidden="true">🔁</span>
              2025-26冬 主な移籍一覧
            </a>
          </h3>
          <div class="transfer-sub">
            冬の主要選手の移籍はコチラから一覧で！<br />
            更新：<span class="date-red">{transfersUpdatedAt}</span>
          </div>
        </div>
      </div>

      <div class="footer">
        ※ 各更新日は、代表・クラブそれぞれの最新データ反映日を示しています。
      </div>

      <div
        style="margin-top:28px; text-align:center; font-size:13px; color:#666;"
      >
        <a href="/about">World Football Archive について（このサイトの概要）</a>
      </div>
    </div>
  </div>
</BaseLayout>
